name: release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  runtime:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Run tests
        run: npm test

      - name: Build source
        run: npm run build

      - name: Package runtime tarball
        run: npm run release:runtime

      - name: Upload release asset
        uses: softprops/action-gh-release@v2
        with:
          files: .release/auto-resume-runtime.tar.gz
          overwrite_files: true
