{
  "name": "auto-resume",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/opencode.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "prepare": "npm run build",
    "test": "node --test --import tsx",
    "release:runtime": "node scripts/package-runtime.mjs --out .release/auto-resume-runtime.tar.gz"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.3"
  },
  "dependencies": {
    "@opencode-ai/sdk": "latest"
  }
}
